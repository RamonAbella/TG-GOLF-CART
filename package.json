{
  "name": "tg-golf-carts",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "install:all": "npm install && npm install --workspace=client && npm install --workspace=server",
    "setup": "npm run install:all && npm run db:setup --workspace=server",
    "db:setup": "npm run db:push --workspace=server && npm run db:seed --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "puppeteer": "^24.42.0"
  }
}
